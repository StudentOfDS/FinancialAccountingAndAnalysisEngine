# Forecasting Methodology

Forecasts use data quality checks, baseline models, time-based backtesting, model comparison by validation error, residual prediction intervals and plain-English explanations. ML models are skipped unless enough observations exist.
