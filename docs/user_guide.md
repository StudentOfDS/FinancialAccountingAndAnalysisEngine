# User Guide

Upload CSV or Excel files using the Streamlit app. Financial Analyst Mode requires the statement schema shown in `data/financial_statements_sample.csv`. Accounting Builder Mode can be run from `AccountingService` using sample transaction, opening balance and asset files.
